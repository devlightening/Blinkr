# Devir notu — 2026-09-24

Devam paketi (Faz A–G) kodda tamamlandı ve `feat/blinkr-theme-redesign` dalına push edildi. Ayrıntı:
`PROGRESS_DEVAM.md`, kararlar `docs/sinyal-mvp-plan/docs/plan/DECISIONS.md` D-016…D-022.

## Commit'ler (bu oturum)
| Commit | Faz |
|---|---|
| 755a826 | D — tek sayfa composer, gönder hedefleri, çevrimdışı giden kutusu, kamerada yer çipi, "Galeriden" |
| 64eba69 | E — balon sohbet, gruplama, gün ayırıcı, Görüldü, yazıyor, alıntılı yanıt |
| f602614 | F — hesap silme (30 gün + tüm servislerde), doğum yılı/18 altı, yasal metinler, izin metinleri |
| 9adf362 | G — tam tr/en, dil ayarı, i18n bekçisi |
| 974298f | G — erişilebilirlik, analitik, gecikme ölçümü, demo seed, Maestro, yayın listesi |

## Kullanıcının yapması / karar vermesi gerekenler
1. **Destek/itiraz e-postası** — `src/Clients/Blinkr.Expo/src/legalContent.ts` → `SUPPORT_EMAIL` (şu an `{{DESTEK_EPOSTA}}`). Mağaza yayınını engeller.
2. **Test verisi silme** (A2/A3): `node scripts/cleanup-test-data.cjs --backup`; ahmet seed'i için BlogService'i `RateLimiting__GlobalPermitLimit=30000` ile açıp `node scripts/cleanup-synthetic.cjs --only ahmet`.
3. **Kökteki `.env` git'te izleniyor** (yerel kimlik bilgileri): izlemeden çıkarıp parolaları değiştir.
4. Secret/ücretli: Sentry DSN, PostHog anahtarı (analitik şu an no-op), APNs/FCM.
5. Yasal metinler taslak — hukuki inceleme.

## Cihazda doğrulanacaklar
- İki fiziksel cihaz kabul testi (CLAUDE.md §19.3), `e2e/maestro` akışlarının ilk koşusu.
- Uçak modunda paylaş → bağlantı gelince yükleniyor mu; kamerada yer çipi; sohbette klavye; açılış/kamera/kart/fps ölçümleri.

## Yeni kabul betikleri
`test-signal-card.ps1` (fromGallery dahil), `test-chat-thread.ps1` (BLK-CHAT-03), `test-account-lifecycle.ps1` (BLK-ACCOUNT-01), `measure-api-latency.ps1`, `seed-demo.cjs`.

## Çalışma kuralları (değişmedi)
`api.ts` içindeki yerel IP commit'lenmez (commit öncesi .106'ya çevir, sonra .35'e geri al); `git add -A` sonrası özel dosyalar `git reset`; yeni metin `tx()` ile ve tr+en; `npm run test:i18n` kodda metin bırakmaz.
