# HANDOFF — 2026-09-23 (b): Faz 10 ortası, kullanıcı test ediyor

> Bir önceki devir: `HANDOFF_2026-09-23.md`. Bu dosya ondan sonra yapılanları ve **tam olarak nerede durulduğunu** anlatır.
> Kullanıcı bu noktada uygulamayı test etmek için durmamı istedi; test bitince buradan devam edilecek.

## 1. Durum özeti

- Aktif faz: **Faz 10 — güvenlik, gizlilik, moderasyon** (yarısından fazlası bitti).
- Faz 4-9 tamamlandı (bkz. PROGRESS.md faz özetleri).
- Dal: `feat/blinkr-theme-redesign`, her adım commit + push edildi. Çalışma ağacında yalnız kullanıcının kendi dosyaları ve `api.ts`'teki yerel IP satırı var (aşağıya bak).
- Durma anında **her şey yeşil**: tüm backend kabul betikleri, `typecheck`, `test:nearby`, `test:ui`, `test:i18n`, `test:theme`, iOS + Android `expo export`.

## 2. Bu oturumda yapılanlar (commit sırasıyla)

| Commit | Görev | Kanıt |
| --- | --- | --- |
| `3c9beb3` | P10.8 Loglarda koordinat yok: ~30 log satırı temizlendi; çerçeve istek/HttpClient/YARP logları Warning; PlaceService kapsama anahtarı logda tek yönlü kimlik | `scripts/test-log-privacy.ps1` (BLK-LOGPRIV-01) |
| `9546d10` | P10.1 Metin filtresi `Shared.Moderation.ContentTextFilter`: tehdit/nefret/hedefli hakaret 422 `CONTENT_BLOCKED`; TC no. + plaka maskeleme (herkese açık metin); hafif küfür Keşfet'te ×0,5 + "Hassas içerik"; uygulamada kişisel veri uyarısı (`textSafety.ts`, `PersonalDataNotice`) | `tests/ContentFilter` (BLK-TEXTFILTER-01), `scripts/test-text-filter.ps1` (BLK-TEXTFILTER-02) |
| `5b2fbf5` | P10.3/P10.4 Ağırlıklı raporlar, otomatik gizleme (skor ≥ 3), admin kuyruğu, yaptırım merdiveni, denetim izi, `ModerationNotice` bildirimi; PlaceService'te silinen sinyalin canlı durumda kalma hatası düzeltildi | `scripts/test-moderation.ps1` (BLK-MODERATION-01) |
| (bu commit) | P10.9 Yetkilendirme test paketi; bulunan hata: başkasının gönderisini düzenleme/silme 500 dönüyordu → artık 403 `FORBIDDEN` (BlogService `ExceptionMiddleware`) | `scripts/test-authz.ps1` (BLK-AUTHZ-01) |

Kararlar: DECISIONS.md **D-013** (log gizliliği), **D-014** (metin filtresi), **D-015** (moderasyon tasarımı, P10.2 ertelendi).

## 3. Kalan işler (sıra ile)

1. **P10.10** Topluluk kuralları, kullanım şartları, gizlilik politikası ekranları (Ayarlar'dan; metinler yer tutucu + "hukuki inceleme gerekir" notu; i18n tr/en). Buna destek/itiraz iletişim bilgisi de bağlanmalı: **gerçek bir destek adresi yok, uydurma adres yazma**; yer tutucu olarak işaretle ve D-016'ya yaz.
2. **P10.7** Hesap silme (2 adım, 30 gün bekleme, purge işi, geri alma; veri indirme talebi). Kapsamlı: Identity + Blog (sinyaller için PostDeleted) + Notifications (sohbet "Silinmiş kullanıcı", snap/hikâye medyası). Yarım silme yapılmamalı (kök CLAUDE.md §20.1). Yapılamayan kısımlar DECISIONS'a.
3. **P10.6** 18 yaş altı varsayılanları: bugün doğum yılı toplanmıyor. Ya kayıt/onboarding'e doğum yılı ekle ve varsayılanları uygula, ya da gerekçesiyle ertele (DECISIONS).
4. PROGRESS'te Faz 10 özetini yaz, fazı kapat.
5. **Faz 11** (i18n taraması: ReportPanel, EditProfileSheet, SignalComposer gibi hâlâ sabit Türkçe metinli ekranlar; a11y; reduce motion; performans; analitik soyutlaması (konum/metin göndermeden); onboarding).
6. **Faz 12** (seed veri, E2E, Sentry → DSN secret olduğu için ertele, mağaza varlıkları, izin metinleri, yayın kontrol listesi).
7. Faz 13 isteğe bağlı (V1.1).

## 4. Çalışma kuralları (unutma)

- **Yerel IP:** `src/Clients/Blinkr.Expo/src/api.ts` içindeki `'http://192.168.1.35:5080'` asla commit edilmez. Commit'ten önce `'http://192.168.1.106:5080'` (HEAD değeri) yapılır, commit sonrası `.35` geri konur.
- `git add -A` sonrası mutlaka: `git reset -q -- BLINKR_UYGULAMA_PLANI.md docs/BLINKR_MASTER_VIZYON_URUN_VE_BETA.md docs/blinkr_tema_kod/ docs/new/` (kullanıcının özel dosyaları).
- Commit mesajı sonu: `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`; her commit sonrası push.
- Secret/ücretli servis/veri silen migration dışında kullanıcıya soru sorma; her sapma DECISIONS.md'ye.
- Kullanıcıya görünen yeni her metin i18n (tr + en); renk/ölçü yalnız `theme.ts` token'ları.
- PowerShell 5 BOM'suz betikleri ANSI okur: Türkçe karakterli `.ps1` testlerine UTF-8 BOM ekle.
- PowerShell değişkenleri büyük/küçük harf duyarsız (`$Raw` ≠ ayrı değişken değil `$raw`).
- Worker `PostDocument`'a alan eklenirse BlogService'teki **iki** `PostDocument` kopyasına da ekle (yoksa Blog okumaları 500 verir — bu oturumda yaşandı, düzeltildi).
- Identity'de `[Authorize(Roles=...)]` çalışmaz (JwtBearer "role" → `ClaimTypes.Role` eşlemesi); `AdminModerationController.AdminPolicy` gibi iki türü kabul eden politika kullan.
- Admin yapmak: `scripts/make-admin.ps1 -User <e-posta>`, moderasyon: `scripts/moderation.ps1 queue|resolve|actions`.

## 5. Ortam

- Servisler yerel dotnet süreçleri (Identity 5188, Blog 5215, Place 5225, Notifications 5290, Gateway 5080), worker Docker (`blinkr_projections_worker`). Loglar `artifacts/dev-logs/*.stdout.log`.
- Tek servisi yeniden başlatma yardımcıları oturumun scratchpad'indeydi; yoksa `scripts/start-blinkr-dev.ps1` kullan. Worker: `docker compose up -d --build projections-worker`.
- Identity'ye yeni migration eklendi: `20260923132726_AddModerationAndSanctions` (yalnız ekleme; başlangıçta otomatik uygulanır).
- Tam kabul koşucusu: `scripts/test-product-08.ps1` (yeni testler listede: log-privacy, text-filter, moderation, authz + `tests/ContentFilter`).

## 6. Kullanıcının test edebileceği yeni davranışlar

- Composer/yorum/bio'da telefon, TC no., plaka ya da adres yazınca uyarı kutusu; paylaşınca TC no. ve plaka `•` ile gizlenir.
- Tehdit/nefret içeren metin reddedilir ("topluluk kurallarına uymuyor").
- Keşfet'te hafif küfürlü sinyalde "Hassas içerik" etiketi.
- Bildir ekranında yeni nedenler; "Kendine zarar verme" seçilince acil numara bilgisi.
- Bir sinyal yeterince raporlanınca haritadan/akıştan/yer durumundan kalkar; admin CLI ile geri alınabilir.
- Kısıtlanan hesap paylaşım yapamaz, askıya alınan hesap giriş yapamaz; bildirim listesinde uyarı görünür.
